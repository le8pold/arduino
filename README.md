This project consists of a website, python script, and arduino.
basically, the website will let you submit a message, which will
be put into a txt file, which then a cloudflare worker fetches,
and my python script fetches from the worker. then the arduino
listens to the python script, and once the message is sent,
the python scirpt sends the message to the arduino's lcd display.
theres also hardware monitoring on the arduino. fun little project,
I found and old arduino kit and thought itd be fun to make 
somrthing like this.
volundtyr is my little worker lol. will probably try to make this readme a bit better looking and understanding later but i mean whos reading ts anyways
